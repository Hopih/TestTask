# Copy to `.env` in the repo root. Do not commit the real `.env`.

# Used by docker compose and by a local `dotnet run`.
POSTGRES_PASSWORD=testtask

# Optional CRM key override for Docker / the API.
INTEGRATION_API_KEY=local-crm-key
