# Copy to `.env` in this folder if you prefer env vars over user-secrets.
# Do not commit the real `.env`.

POSTGRES_PASSWORD=your_postgres_password

# Optional full override:
# ConnectionStrings__Postgres=Host=localhost;Port=5432;Database=testtask;Username=postgres;Password=your_postgres_password
