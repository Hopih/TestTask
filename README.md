# TestTask — стол заявок

Небольшой внутренний сервис учёта заявок: форма, список, статусы, фильтр и сохранение в PostgreSQL. Рядом — один интеграционный endpoint, через который внешняя CRM может создать заявку и получить список.

## Как запустить проект

### Docker (проще всего)

Нужны **Docker Desktop** и Compose. Свой PostgreSQL на машине не обязателен — в контейнере поднимается отдельная база.

```powershell
cd C:\TestTask\TestTask
copy .env.example .env
docker compose up --build
```

Откройте **http://localhost:8080**. API доступен и напрямую: `http://localhost:5080`.

Демо-пароль БД и ключ CRM заданы в `.env.example`. Для чего-то кроме локального просмотра смените их в `.env` (файл в `.gitignore`).

Остановка: `docker compose down`. Данные заявок живут в volume `pgdata`.

### Локально, без Docker

Нужны **.NET 10 SDK**, **Node.js 20+** и локальный **PostgreSQL** (порт `5432`).

1. Создайте базу, если её ещё нет:

```sql
CREATE DATABASE testtask;
```

2. Задайте пароль PostgreSQL одним из способов (в Git он не хранится):

В том же терминале, откуда запускаете API:

```powershell
$env:POSTGRES_PASSWORD = "ваш_пароль"
```

Либо один раз положите пароль в `backend/src/TestTask.Api/appsettings.Local.json` (файл в `.gitignore`). Шаблон: `appsettings.Local.json.example`.

Если заданы оба, берётся переменная из терминала.

3. Запустите API из корня репозитория:

```powershell
dotnet run --project backend/src/TestTask.Api
```

API слушает `http://localhost:5080`.

4. В другом терминале поднимите UI:

```powershell
cd frontend
npm install
npm.cmd run dev
```

Откройте `http://localhost:5173`. Vite проксирует `/api` на бэкенд.

### Интеграция с CRM

Один ресурс: `/api/integration/leads`.

| Метод | Назначение |
| --- | --- |
| `GET /api/integration/leads` | Список заявок, опционально `?status=New\|InProgress\|Success\|Rejected` |
| `POST /api/integration/leads` | Создать заявку |

Ключ передаётся заголовком `X-Api-Key`. Для локального демо в `appsettings.json` стоит `local-crm-key` — это не секрет продакшена, перед выходом во внешнюю сеть его нужно сменить.

Готовые запросы лежат в `backend/src/TestTask.Api/TestTask.Api.http`.

## Какой стек использовал

- **Backend:** ASP.NET Core 10 Web API, контроллеры, EF Core, Npgsql
- **Frontend:** React 19, TypeScript, Vite
- **БД:** PostgreSQL (локально или контейнер)
- **Интеграция:** один REST-ресурс и заголовок `X-Api-Key`
- **Упаковка:** Docker Compose (API + UI + Postgres)

## Какие AI-инструменты использовал

Разработка шла в **Cursor**. Другие агенты (Claude Code, Codex) для этого репозитория не подключались.

## Для каких задач применял AI

- Собрать каркас репозитория, ASP.NET API и React-приложение
- Спроектировать модель заявки, статусы и общий сервис для UI и CRM
- Набросать интерфейс, валидацию и прокси Vite
- Поднять подключение к локальному PostgreSQL и проверить API

## Что делал самостоятельно

- Сформулировал ТЗ, выбрал стек ASP.NET + React и локальный PostgreSQL
- После генерации **самостоятельно рефакторил код**: вынес работу с заявками в сервис, отделил CRM-контроллер от UI, поправил контракты и валидацию
- **Корректировал руками часть компонентов**: форма заявки, выпадающий список источника, плашки статусов, фильтры и вёрстка списка
- Убрал лишние элементы интерфейса, подобрал источники и довёл внешний вид до рабочего внутреннего инструмента

## Какие решения принимал и почему

- **Один путь `/api/integration/leads`** с `GET` и `POST`, а не два разных URL: так выглядит «один endpoint» для внешней системы, при этом HTTP-методы остаются привычными.
- **Общий `LeadService`** для UI и CRM: интеграция не дублирует логику, её можно ужесточить отдельно (ключ, лимиты, аудит).
- **Пароль БД только в `POSTGRES_PASSWORD` / `.env`**, в репозитории его нет.
- **`EnsureCreated` вместо миграций:** для мини-сервиса быстрее поднять схему; для долгой жизни проекта миграции надёжнее.
- **Фильтр статусов в UI на клиенте**, `?status=` на API: в интерфейсе нужны счётчики по всем статусам, CRM может забирать узкую выборку.
- **Docker Compose из трёх сервисов:** отдельная Postgres, API и nginx с UI. Nginx проксирует `/api`, поэтому браузер ходит на один порт. Порт Postgres на хост не пробрасывается, чтобы не конфликтовать с уже установленным сервером.

## Что бы улучшил при наличии дополнительного времени

- Вход для сотрудников, роли и журнал смены статусов
- EF Core миграции, пагинацию, поиск по телефону и имени
- Нормализацию телефона и защиту от дублей
- Тесты API и пару сценариев на форму
- Вынести ключ CRM в секрет-хранилище, добавить rate limit и идемпотентность
- CI, миграции EF Core и отдельный секрет-хранилище для ключа CRM
